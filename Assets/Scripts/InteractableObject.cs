using UnityEngine;

public class InteractableObject : MonoBehaviour
{
    //if interacted hide object and add score 
    //wait xAmount of seconds and then show object again
    //subscribe to input manager for interaction input
}
